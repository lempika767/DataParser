 create the plan for a skill which will read very big files from parameter path. File contains  numbers maximum 2 decimals: like 15, 67. We need to read the data from file in batches in those files we need to find data templates : it at least 2 numbers but it could be up to 15 same sequence of numbers. As a result create new file where same patterns replaced with $ref-id , where ref-id is integer - patterns are stored in separated file. Some patterns could be wider then in batch but in file. after the first patterns replacement it recheck again the file an dtry to find more or wider patterns. And doing this per batch at least 10 times and every time creating new file if at least one replacement found. all previous versions deleted. in order to find patterns on borders of batches it should change batch size for every iteration. as a second phase it compare and try to find bigger patterns between batches so now we compare not only numbers in one batch , but numbers in different batches. at the end recheck references ack in batches - maybe smaller patterns were replaced with bigger patterns - and no  any usage any more, then we need to delete it



❯ create plan how to improve skill so at the end it will show memory saving percentage of result file + patterns in comparison with original file